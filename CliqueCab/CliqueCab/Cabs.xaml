<Page
    x:Class="CliqueCab.Cabs"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CliqueCab"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

	<Grid x:Name="CabsMainGrid">
		<TextBlock x:Name="PassengerCapacity" Width="380" FontSize="30" HorizontalAlignment="Center" TextAlignment="Center" Margin="10,24,10,511"/>

		<ListView x:Name="CabsListView"	Margin="10,74,10,456" ItemsSource="{Binding}" IsItemClickEnabled="True" ItemClick="CabsListView_ItemClick" Height="50"  ScrollViewer.HorizontalScrollBarVisibility="Auto"	ScrollViewer.VerticalScrollBarVisibility="Disabled" ScrollViewer.HorizontalScrollMode="Auto" ScrollViewer.VerticalScrollMode="Disabled" Background="DarkSlateGray" >
			<ListView.ItemsPanel>
				<ItemsPanelTemplate>
					<VirtualizingStackPanel Orientation="Horizontal" />
				</ItemsPanelTemplate>
			</ListView.ItemsPanel>
			<ListView.ItemTemplate>
				<DataTemplate>
					<Grid>
						<Image Grid.Row="0" Source="{Binding Image}" Width="60px" Height="60px" Margin="20,0"/>
						<TextBlock Grid.Row="1" Text="{Binding Display_Name}" HorizontalAlignment="Center" />
					</Grid>
				</DataTemplate>
			</ListView.ItemTemplate>
		</ListView>

		<ListView x:Name="ListOfCabs" AllowDrop="True" Drop="ListOfCabs_Drop" Margin="10,143,10,34" ItemClick="ListOfCabs_ItemClick" Background="DarkGray">
			<ListView.ItemTemplate>
				<DataTemplate>
					<StackPanel Orientation="Horizontal">
						<TextBlock Text="{Binding Display_Name}" />
						<Image Source="{Binding Image}" Width="60px" Height="60px" Margin="20,0"/>
					</StackPanel>
				</DataTemplate>
			</ListView.ItemTemplate>
		</ListView>

	</Grid>

	<Page.BottomAppBar>
		<CommandBar>
			<AppBarButton x:Name="BookCabsBtn" Label="Book Cabs" Icon="Accept" />
		</CommandBar>
	</Page.BottomAppBar>
</Page>
